{
  "name": "ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite serve --open true --strictPort true --port 5173",
    "start:nightly": "vite serve --mode nightly --open true --strictPort true --port 5173",
    "build:dev": "tsc && vite build --minify false --sourcemap true --outDir ./../wwwroot -w",
    "build": "tsc && vite build --outDir ./../wwwroot",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/user-event": "^14.5.2",
    "@types/jest": "^29.5.14",
    "@vitest/coverage-v8": "^2.1.8",
    "@vitest/ui": "^2.1.8",
    "happy-dom": "^15.11.7",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
